/**
 * Explore: public places worth wandering into.
 * Placeholder — being built out against GET /conversations/discover.
 */
export function ExploreScreen() {
  return <div className="chat-empty">Explore is coming together…</div>;
}
