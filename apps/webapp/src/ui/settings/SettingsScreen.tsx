/**
 * Settings: account, profile, notifications, what's new.
 * Placeholder — being built out against PATCH /users/me and friends.
 */
export function SettingsScreen() {
  return <div className="chat-empty">Settings are coming together…</div>;
}
