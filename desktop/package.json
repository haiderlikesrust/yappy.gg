{
  "name": "yappy-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "yappy for desktop — a native Rust (Tauri) shell around app.yappy.gg",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
