<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is blocked by default from Android 9 onwards, which is correct
  and stays on for everything.

  The two exceptions below are the loopback addresses a local backend runs on:
  10.0.2.2 is the host machine as seen from the emulator, and localhost covers
  `adb reverse` on a physical device. Neither is reachable from outside the
  machine, so permitting cleartext there costs nothing — and scoping it this way
  is what avoids the usual shortcut of setting `usesCleartextTraffic="true"`
  application-wide and shipping it to production by accident.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
