org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
# Pin the build JVM: whatever JAVA_HOME happens to be (currently a JDK 25 that
# this Gradle version cannot parse), the build always runs on a supported JDK.
org.gradle.java.home=C:/Program Files/Java/jdk-21.0.10
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official
